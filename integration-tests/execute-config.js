const folders = {
	registerRepo: 'cli-utility',
	generateTestFolder: 'generate-from-list',
	generateUrlTestFolder: 'generate-from-url',
	generateWithConfigFolder: 'generate-with-config'
};

module.exports = {folders};
